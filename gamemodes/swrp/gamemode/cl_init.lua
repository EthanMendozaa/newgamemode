--[[----------------------------------------------------------------------------
	SWRP — client bootstrap

	shared.lua was sent to us via AddCSLuaFile on the server. Run it; it boots
	the module loader, which pulls in every cl_/sh_ file the server flagged.
------------------------------------------------------------------------------]]

include( "shared.lua" )
