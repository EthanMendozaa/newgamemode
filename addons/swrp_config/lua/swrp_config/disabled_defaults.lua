--[[----------------------------------------------------------------------------
	SWRP config — disabled defaults

	Turn off a shipped default (battalion, class, rank ladder, cert...) before
	replacing it with your own, so the two don't collide. Loaded first, before
	any definitions.

	Example:
	    SWRP.Config.Disable( "501st Legion" )

	The gamemode ships no defaults yet, so this file is empty. It exists so
	server owners always have a single, obvious place to disable things later.
------------------------------------------------------------------------------]]

-- SWRP.Config.Disable( "Example Battalion" )
